services:
  mongo:
    image: mongo:7
    container_name: xpress-code-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: xpress_code
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
